server:
  port: 8182

spring:
  application:
    name: employee-service

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8661/eureka
    instance:
      hostname: localhost

feign:
  name: employee-service
  url: /api/employees